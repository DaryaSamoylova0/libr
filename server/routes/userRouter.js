const Router = require('express')
const router = new Router()

router.post('/registration', )           //создавать
router.post('/login', )                  //получать
router.get('/auth', )

module.exports = router